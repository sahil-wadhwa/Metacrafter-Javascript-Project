import './Productitem.css';
import Card from './Card';

const ProductItem=(products)=>{
    return(
        <Card className="product-item">
            <ProductDate date={PaymentResponse.date}/>
        </Card>
    )
}